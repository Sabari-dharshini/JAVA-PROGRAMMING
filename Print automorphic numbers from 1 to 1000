public class Main{
    public static void main(String[] args) {
        

        for (int num = 1; num <= 1000; num++) {
            int square = num * num;
            int temp = num;
            int mod = 1;

            
            while (temp > 0) {
                mod *= 10;
                temp /= 10;
            }

           
            if (square % mod == num) {
                System.out.print(num + " ");
            }
        }
    }
}
